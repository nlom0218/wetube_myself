extends layouts/main
include mixins/videoBlock

block content
    p Hello Everyone!!! It's PUG~!~!~!
    .video
        each item in dbvideos
            +videoBlock({
                videoFile:item.videoFile,
                id:item.id,
                title:item.title,
                views:item.views
            })