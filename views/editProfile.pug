extends layouts/main

block content
    p Edit Profile
    .form-container
        form(action=`/users${routes.editProfile}`, method="post")
            label(for="avatar") Avatar
            input(type="file", id="avatar", name="avater")
            input(type="text", placeholder="Email", name="email")
            input(type="text", placeholder="Nickname", nmae="nickname")
            input(type="submit", value="Update Profile")
    a.form-container__link(href=`/users${routes.changePassword}`) Change Password