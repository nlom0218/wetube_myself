extends layouts/main
include mixins/videoBlock

block content
    h3 Seraching for : #{searchingBy}
    .video
        each item in dbvideos
            +videoBlock({
                videoFile:item.videoFile,
                id:item.id,
                title:item.title,
                views:item.views
            })